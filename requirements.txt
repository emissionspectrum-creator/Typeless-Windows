# Typeless —— Windows / Qwen3-ASR CPU 路線
# 實測可用的版本組合（Python 3.13.13 / Windows 11）。版本全部釘死。
#
# 模型與推論引擎不在這裡：它們來自 c:\dev\qwen3-asr，
# 由 typeless.py 開頭的 ASR_HOME 指過去。見 README.md。
onnxruntime==1.24.4
numpy==2.4.6
soundfile==0.14.0
sounddevice==0.5.6
OpenCC==1.4.1
gguf==0.19.0
srt==3.5.3
